# Claude Fable 5 repository instructions

この文書は`AGENTS.md`を元に、Claude Fable 5の長期タスク遂行、自己検証、先回りして問題を見つける特性に合わせて調整した指示である。

## 作業の進め方

- 利用者の目的、対象範囲、完了条件を先に特定し、その範囲内では不要な確認待ちを挟まず作業を進める。
- 長いタスクは実装可能な段階へ分ける。ただし、計画の更新や進捗文書の作成自体を仕事にせず、判断や引き継ぎに必要な量へ抑える。
- 未知のコードでは推測から書き始めず、関連する実装、設定、テスト、変更中の差分を確認してから編集する。
- Fable 5が長期間自律的に作業できることを、依頼範囲を広げる理由にしない。隣接する改善案を見つけても、現在の目的に必要でなければ実装せず候補として報告する。
- 複数の独立作業を並行化すると所要時間が明確に短くなる場合だけサブエージェントを使う。小さな作業を分割したり、同じ調査を重複させたりしない。

## 設計の判断

- 実装速度、保守性、品質、性能、検証コストのバランスを取り、要件とリスクに対して妥当な設計を選ぶ。
- 将来使うかもしれない抽象化、汎用基盤、依存関係、設定項目を過剰に先回りして追加しない。一方で、明確な保守性や性能上の効果がある設計まで機械的に削らない。
- 既存実装の局所修正とアーキテクチャ変更を比較し、後者の複雑さに見合う具体的な効果がある場合だけ置き換える。
- 性能最適化は測定結果またはコード上で確認できるボトルネックへ絞る。計測専用コードや一時的な診断処理を製品へ不要に残さない。
- Fable 5の自己レビューは一度の修正を完成させるために使う。新しい論点を際限なく増やし、全面改修へ発展させない。

## テストと完了条件

- テストは禁止ではない。変更規模、影響範囲、回帰リスクに応じて、実装と検証のバランスを取る。
- テストを追加または実行する前に、どの不具合を検出するためかを判断する。目的が重複する試験や、結果が判断を変えない試験は増やさない。
- 原則として、変更したロジックの対象テスト、静的解析、変更が触れるビルド境界を確認する。高リスク変更、重大障害、リリース判定では範囲を広げる。
- 1日の作業時間の約9割をテストの追加や実行だけに費やす配分は、利用者の明示要求または重大なリスクがない限り避ける。
- テスト時間が実装時間を大きく上回り始めたら、残る試験が検出する具体的なリスクと所要時間を再評価し、価値の低い網羅試験を止める。
- 自己検証は、主要な受け入れ条件と変更固有の回帰リスクを確認できた時点で完了する。同じ成功を別手段で繰り返し証明しない。
- テスト成功を、未計測の実機性能、外部サービスの状態、手動操作の受け入れ結果と混同しない。未確認事項は明記する。

## ツールと変更範囲

- 読み取り専用の調査は可能な範囲でまとめ、同じファイルや状態を理由なく何度も確認しない。
- 利用者の既存変更と無関係な差分を上書きしない。変更中の作業ツリーでは、今回触れた範囲を区別して扱う。
- 新しい文書、補助スクリプト、ベンチマーク基盤は、成果物として必要な場合だけ追加する。
- 完了時は、変更内容、実施した検証、未確認事項を簡潔に報告する。作業過程を長く再掲しない。

## バージョン管理

- 利用者から明示的な許可がない限り、アプリのバージョン番号とビルド番号を変更しない。
- リリース作業を依頼されても、変更後のバージョンが明示されていない場合は、版番号を推測せず利用者へ確認する。
- バージョンは`X.Y.Z`形式で管理する。
- 中規模リリースではマイナーバージョンを1つ上げ、パッチバージョンを0へ戻す。これは数値表現で`+0.1`に相当する（例：`0.2.1`から`0.3.0`）。
- 小規模リリースではパッチバージョンを1つ上げる。これは数値表現で`+0.01`に相当する（例：`0.2.1`から`0.2.2`）。
- メジャーバージョンの変更は規則から推測せず、必ず利用者の明示的な指定を受ける。
- 一度の許可はそのリリースだけに適用し、将来のリリースや別の変更への許可として扱わない。
- 許可された版番号へ変更するときは、`pubspec.yaml`、アプリ内表示、文書、成果物名、タグ、リリース名の表記を一致させる。
