# VBTerminal テスト記録

この文書だけが、評価に使用した端末名、OS固有機能、エミュレーター構成、実測値を扱う。一般向けのREADMEと設計判断は特定端末へ依存させない。

## AndroidエミュレーターとWSL

- API 35のAndroid Emulatorを`-gpu host`で起動し、WSL内の隔離sshdへ`adb reverse`で接続した。
- SwiftShaderではxterm.jsのグリフアトラスが欠けるため、WebGL描画の合否判定には使用しない。
- パスワード認証、PTY、`top`全画面表示、接続中タブの復元、22.3秒間のホスト鍵確認待機を確認した。
- 縦画面とIME表示中にPTYを19行47列から20行53列へ拡張できた。

## 性能測定

2026年8月11日のprofileビルドでは、8秒間の連続出力で次を確認した。

- SSH受信ウィンドウ通知のCPU inclusive比率: 約64.5%から0.2%
- 高Hzパルス経路: 約5.5%から0.1%
- 接続中アイドルPSS: 297.0MiBから264.1MiB
- 大量出力ピークPSS: 342.5MiBから294.8MiB
- AES-128-CTRとHMAC-SHA256-ETMのBoringSSL FFI化: 4.15MiB/sから6.48MiB/s
- CPU効率: 0.336秒/MiBから0.229秒/MiB
- AES-128-GCM固定: 1.28MiB/s

### UI遷移とWebGLターミナル

2026年8月11日にAPI 35、60Hz、ホストGPUのAndroid Emulatorとprofileビルドで再測定した。SurfaceFlingerのFlutter SurfaceView実提示時刻、`/proc`のCPU時間、`dumpsys meminfo`を使用した。`dumpsys gfxinfo`はFlutterのフレームを取得できないため使用していない。

- SSHタブ切替をルート遷移から単一WebViewのセッション差し替えへ変更した。24タブ保持時の8往復で、CPUは96.25%から73.37%、p50は50.74msから17.54ms、33.3ms超の間隔は78.1%から8.0%へ低下した。
- ボトムナビの15往復ではp95がSSH・FTP間35.94ms、FTP・設定間34.54ms、設定・SSH間34.97msだった。変更前は順に36.59ms、36.35ms、38.40msだった。
- SSH受信の可視バッチを8msから16msにまとめた。10秒間の`yes`出力でアプリCPUは101.87%、WebView合算108.94%だった。変更前の参考値は116.54%と120.42%である。開始タイミングを遅延コマンドで固定したため、CPU差は傾向として扱う。
- 大量出力時のPSSはアイドル211.4MiB、ピーク233.1MiB、停止後211.8MiBだった。変更前のピーク295.4MiBに対し、一時増加を80.6MiBから21.7MiBへ抑えた。
- 大量出力中のフレーム間隔は依然として最大の残課題である。CPUとメモリは改善したが、連続出力中のp95は約99msで、通常のTUI操作とは分けて評価する必要がある。

## Rust SSH・SFTPコア

2026年8月11日にAPI 35、60Hz、ホストGPUのAndroid Emulatorとprofileビルドから、WSL内の隔離sshdへ接続した。Rustコア経由のパスワード認証、鍵交換、PTY開始、シェル出力、SFTPのホームディレクトリ一覧取得を確認した。

アプリ本体とWebView rendererを合算した10秒測定は次のとおりである。保存済みタブ記述子は27件だが、WebViewと接続中SSHセッションは各1件である。

| 項目 | Rust移行後 | 直前のDart実装記録 |
|---|---:|---:|
| 接続中アイドルCPU | 3.1% | 未計測 |
| 接続中アイドルPSS | 256.1MiB | 211.4MiB |
| 10秒連続出力CPU | 135.1% | 108.9% |
| 連続出力ピークPSS | 287.9MiB | 233.1MiB |
| 出力停止10秒後PSS | 285.0MiB | 211.8MiB |

連続出力は`timeout 10s yes 0123456789abcdef`を使用した。SurfaceFlinger TimeStatsではWebGLのSurfaceViewが平均6.8fps、FlutterのActivity面が平均9.1fpsだった。無制限出力時の表示更新とWebView側履歴保持は引き続き最大のボトルネックである。

Windowsホストから同じWSL sshdへ直接接続してRustコアだけを補助測定した。SFTP接続は561.39ms、同一セッションで100回行ったディレクトリ一覧はp50 1.25ms、p95 1.44msだった。SSHを8セッション連続確立した所要時間はp50 157.22ms、p95 188.38msだった。この結果から、SFTP一覧とセッション管理自体は低遅延だが、Androidアプリ全体ではFFI境界以降の出力変換、WebView描画、スクロールバック保持のコストが支配的と判断する。

Rust移行だけでは大量出力時のアプリ全体性能は改善せず、今回の条件ではCPUとPSSが悪化した。測定用の一時コードは製品ソースへ残していない。

### 150MiB目標に向けた再設計

次の3案を比較した。

1. RustとDartのキューだけを縮小する案は工数が小さい。Rust受信を2MiBから512KiB、Dart未描画データを2MiBから256KiBへ縮小し、192KiBで受信を止めて64KiBで再開する方式にした。WebGL profile測定の大量出力増分PSSは31.2MiBから23.8MiBへ減ったが、アイドルPSSは255.5MiBであり、WebViewの固定費には効かなかった。
2. Flutter描画を既定にしてWebViewを起動しない案は、TUI描画とのトレードオフがあるが効果が最大だった。xterm.js WebGLは設定から選べる高速描画モードとして維持した。
3. Rustからxterm.jsまでUTF-8バイト列を直接渡す案はBase64と文字列コピーを削減できるが、WebViewの固定費約57MiBは残る。IME、再生スナップショット、Flutterフォールバックを全面変更する工数に対して150MiB目標へ届かないため、今回は採用しなかった。

採用構成は、Flutter省メモリ描画、既定2,000行、Rust 512KiB、Dart 256KiB、高低ウォーターマーク式バックプレッシャーである。releaseビルドで再測定した結果は次のとおりだった。

| 項目 | 最終release測定 |
|---|---:|
| 接続中アイドルPSS | 118.5MiB |
| 30秒連続出力中の観測PSS | 115.2〜119.6MiB |
| 連続出力CPU | 88.9% |
| SurfaceFlinger平均FPS | 11.0fps |
| WebView renderer | 0プロセス |

連続出力には`timeout 30s yes 0123456789abcdef`を使用した。150MiB目標はアイドルと連続出力の両方で達成した。複雑なTUIの互換性と描画速度を優先する場合はxterm.js WebGLを選べるが、その場合のアイドルPSSは約255MiBになる。

## 端末操作機能

- エミュレーターで設定画面のカード構成、タブバー、検索ボタン、コピー操作からOSC 133セットアップ案内へ進む導線を確認する。
- 状態付きSSHタブ、検索・コピーの独立した操作領域、多数タブの縦一覧、改善した設定説明をエミュレーターで確認した。
- Noto Sans JP、Koruri、Mejiroがアプリ内ライセンス一覧へ登録されることを自動試験で確認した。
- 検索キー列、TUIマウス、長押し右クリック、OSC 133による出力抽出とプロンプト内カーソル移動は、対応する実アプリケーションとシェルを使った受け入れ試験が必要である。
- 画面スリープ抑止とIMEリサイズはAndroid側へ反映する。長時間動作、画面回転、分割表示は実機で未テストである。

## 実機別記録

### SO-41A

- 公開された表示モードは60Hzのみ。
- 可変リフレッシュレート、120fps、144fpsは未テスト。
- Wi-Fi切断と復帰、画面消灯、バックグラウンド復帰、30分の対話操作、大量出力を今後確認する。

### SCG26

- 120Hz表示モードを公開している。
- 2026年8月9日の事前計測では省電力モードによりOne UIが60Hzへ制限していた。
- Samsung Keyboardの未確定文字、Enter確定、候補領域、フローティング表示、回転時IME再接続は未テスト。
- 120Hz継続、750ms降格、Samsung独自ARRとの協調、DeX、画面消灯30分は未テスト。
