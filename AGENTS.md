# Repository instructions

## GPT-5.6 Sol向けの作業規則

- Solの強みである複雑な推論、意図理解、ツール利用は、利用者が指定した目的を少ない往復で完了するために使う。能力の高さを依頼範囲の拡張や追加機能の先回り実装へ使わない。
- 利用者の意図を推測できても、推測は実装方法の選択にだけ使う。外部への書き込み、破壊的操作、費用の発生、公開、リリース、スコープの実質的な拡張には明示的な許可を必要とする。
- 1,000,000トークンを超える文脈を扱えることを、リポジトリ全体を無差別に読む理由にしない。最初に関連箇所を絞り、判断に必要なファイルと差分だけを読む。
- 長いセッションでは、過去の指示、調査、ツール出力を重複して取り込まない。状態が変わっていない確認コマンドを繰り返さず、現在の目的と完了済み作業を区別する。
- 文脈圧縮や作業再開の直後は、会話上の記憶だけで続行しない。現在の差分、進捗、完了済みの検証、次の一手を一度だけ照合し、完了した編集やコマンドへ戻らない。
- 推論の深さはタスクの難度に合わせる。定型的な編集や明確な局所修正で探索を続けず、難しい設計、性能問題、障害解析で追加推論が結果を改善する場合に時間を使う。
- 小規模で明確な依頼では、関連箇所と制約を確認したら実装へ移る。実装判断を変える新しい証拠がないまま、計画の練り直し、代替案の列挙、追加調査を続けない。
- ツールは結果が次の判断または検証に必要な場合だけ使う。独立した読み取りはまとめてよいが、一回で足りる操作を複数経路で繰り返さない。
- 同じコマンドや修正を条件を変えずに再試行しない。失敗原因を確認してから手段を変え、同種の失敗が2回続いた場合は前提を見直すか、利用者へ未解決点を報告する。
- 完全に読み取り済みで変更されていないファイルは、必要な箇所が文脈から失われた、内容が競合した、または変更を確認する場合を除き、全文を読み直さない。
- 並列作業は、互いに独立した調査や実装へ安全に分割でき、所要時間を実際に短縮できる場合だけ使う。小さな作業、同じファイルを触る作業、結果ごとに次の判断が変わる作業は直列に進める。
- Skillは依頼または作業内容が発動条件へ直接一致するときだけ使う。隣接する能力が便利そうという理由で追加のSkillを連鎖的に起動しない。
- 既存コード、設定、仕様書を正とし、同じ責務を持つ実装パターンを確認してから変更する。根拠のないハードコード、責務を混在させた巨大ファイル、既存構造と競合する新基盤を生成しない。
- Solのフロントエンド設計能力は、UI変更が依頼範囲に含まれる場合に使う。局所的なUI修正を、画面全体の再設計やデザインシステムの追加へ広げない。
- Solは簡潔な回答になりやすいため、最終報告では結論だけに縮めず、変更内容、判断に必要な証拠、未確認事項、次に必要な操作を残す。一般論、作業実況、同じ結論の言い換えは省く。
- セキュリティやデュアルユース領域でセーフガードが介入した場合は、同じ要求を言い換えて迂回しない。正当な防御目的と権限範囲を明確にし、実行できない部分と安全に進められる部分を分けて報告する。
- 未コミットの変更や利用者のデータは、不要に見えても勝手に削除、移動、巻き戻し、クリーンアップしない。破壊的操作は対象を絶対パスと差分で確認し、依頼範囲に含まれない場合は明示的な許可を得る。

## 実装と検証のバランス

- 実装速度、保守性、品質、検証コストのバランスを重視する。単純さだけを優先せず、要件とリスクに対して妥当な設計を選ぶ。
- 将来使うかもしれない抽象化、汎用基盤、依存関係、設定項目を過剰に先回りして追加しない。一方で、明確な拡張性や保守性の効果がある設計まで機械的に削らない。
- 既存実装の修正と新しいレイヤーや方式への置き換えを比較し、複雑さに見合う具体的な効果がある場合だけ採用する。
- 性能最適化は推測だけで広げず、確認できたボトルネックと利用者が指定した目標に直接関係する箇所へ絞る。計測専用コードは製品へ不要に残さない。
- テストは禁止ではない。変更の規模、影響範囲、回帰リスクに見合う必要十分なテストを選び、実装と検証のバランスを取る。
- 作業期間の大半をテストの追加や実行だけに費やさない。例えば、1日の作業時間の約9割がテストになるような配分は、重大な障害対応、リリース判定、利用者の明示要求など合理的な理由がない限り避ける。
- テストが実装時間を大きく上回り始めた場合は、追加テストの目的、検出できる具体的なリスク、所要時間を見直し、重複する試験や価値の低い網羅試験を打ち切る。
- 原則として、変更したロジックの対象単体テスト、静的解析、変更が触れるビルド境界の確認を優先する。無関係な全テスト、長時間の負荷試験、複数環境での網羅試験は、必要性がある場合または利用者が求めた場合に実施する。
- テストを省略した場合も、過剰に実施した場合も、その理由を説明できるようにする。テスト成功を実機性能や未検証の受け入れ条件の達成と混同しない。

## バージョン管理

- 利用者から明示的な許可がない限り、アプリのバージョン番号とビルド番号を変更しない。
- リリース作業を依頼されても、変更後のバージョンが明示されていない場合は、版番号を推測せず利用者へ確認する。
- バージョンは`X.Y.Z`形式で管理する。
- 中規模リリースではマイナーバージョンを1つ上げ、パッチバージョンを0へ戻す。これは数値表現で`+0.1`に相当する（例：`0.2.1`から`0.3.0`）。
- 小規模リリースではパッチバージョンを1つ上げる。これは数値表現で`+0.01`に相当する（例：`0.2.1`から`0.2.2`）。
- メジャーバージョンの変更は規則から推測せず、必ず利用者の明示的な指定を受ける。
- 一度の許可はそのリリースだけに適用し、将来のリリースや別の変更への許可として扱わない。
- 許可された版番号へ変更するときは、`pubspec.yaml`、アプリ内表示、文書、成果物名、タグ、リリース名の表記を一致させる。
